# OG Image Dinámico + Fix Cover Image — Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Cuando se comparte `/e/:identifier/momentos` en WhatsApp, el preview muestra la foto de portada del evento a full con el logo eventiapp en esquina inferior derecha; de paso se corrige que la imagen de portada aparezca rota en el dashboard.

**Architecture:** El backend Go expone un endpoint público `/api/events/:identifier/meta` que devuelve la cover URL resuelta (presigned S3). El dashboard Next.js actualiza `/api/og` para renderizar la foto full + logo cuando hay cover. Astro ya está cableado via `og.ts` — solo necesita la env var `PUBLIC_DASHBOARD_URL`.

**Tech Stack:** Go (Echo + GORM), Next.js 15 Edge Runtime, `@vercel/og` (ya instalado ^0.8.6), `@vercel/og` ImageResponse JSX, AWS S3 presigned URLs

---

## Task 1: Backend — Fix cover_image_url en respuestas de eventos

El problema: `cover_image_url` se guarda en DB como path crudo (`events/uuid.webp`). El dashboard lo renderiza directo y el browser falla.

**Files:**
- Read first: `\\wsl.localhost\Ubuntu\var\www\itbem-events-backend\controllers\events\events.go`
- Read first: `\\wsl.localhost\Ubuntu\var\www\itbem-events-backend\routes\routes.go` (buscar rutas protegidas GET de eventos)
- Modify: el archivo de controllers que serializa eventos para el dashboard

**Step 1: Leer events.go para encontrar dónde se devuelven eventos al dashboard**

```bash
grep -n "func Get" /var/www/itbem-events-backend/controllers/events/events.go
grep -n "cover_image_url\|CoverImageURL" /var/www/itbem-events-backend/controllers/events/events.go
```

Buscar funciones como `GetEvent`, `GetEventByID`, `ListEvents` que devuelvan datos del evento al dashboard (rutas protegidas con JWT).

**Step 2: Agregar helper `resolveCoverURL` en events.go**

En el paquete `events` (mismo archivo o helpers.go nuevo si events.go es grande), agregar ANTES de las funciones que lo usan:

```go
import (
    "strings"
    "events-stocks/configuration"
    "events-stocks/repositories/bucketrepository"
)

// resolveCoverURL convierte un S3 path crudo en presigned URL de 12h.
// Si ya es una URL completa, la devuelve sin cambios.
func resolveCoverURL(rawPath string) string {
    if rawPath == "" || strings.HasPrefix(rawPath, "http") {
        return rawPath
    }
    // rawPath tiene forma "events/uuid.webp"
    parts := strings.SplitN(rawPath, "/", 2)
    if len(parts) != 2 {
        return rawPath
    }
    folder, filename := parts[0], parts[1]
    url, err := bucketrepository.GetPresignedFileURL(
        filename,
        folder,
        configuration.Cfg.AwsBucketName,
        "aws",
        720, // 12 horas
    )
    if err != nil {
        return rawPath
    }
    return url
}
```

**Step 3: Aplicar resolveCoverURL en cada función GET que devuelva eventos**

En cada función que devuelva un evento o lista de eventos al dashboard, después de obtener el/los evento(s) de la DB y antes de la respuesta, agregar:

```go
event.CoverImageURL = resolveCoverURL(event.CoverImageURL)
```

Para listas:
```go
for i := range events {
    events[i].CoverImageURL = resolveCoverURL(events[i].CoverImageURL)
}
```

**Step 4: Build para verificar que compila**

```bash
cd /var/www/itbem-events-backend && go build ./...
```

Expected: sin errores.

**Step 5: Test manual**

```bash
# Desde Windows/WSL, obtener un token JWT del dashboard y hacer:
curl -H "Authorization: Bearer <token>" \
  http://localhost:8080/api/events/<event-id> | jq '.data.cover_image_url'
```

Expected: URL completa que empieza con `https://` (presigned S3 URL). Si antes devolvía `events/uuid.webp`, ahora debe devolver la URL completa.

**Step 6: Commit**

```bash
cd /var/www/itbem-events-backend
git add controllers/events/
git commit -m "fix(events): resolve cover_image_url to presigned S3 URL in GET responses"
```

---

## Task 2: Backend — Nuevo endpoint `/api/events/:identifier/meta`

Endpoint público (sin auth) que devuelve los datos mínimos necesarios para OG tags.

**Files:**
- Create: `\\wsl.localhost\Ubuntu\var\www\itbem-events-backend\controllers\events\meta_controller.go`
- Modify: `\\wsl.localhost\Ubuntu\var\www\itbem-events-backend\routes\routes.go`

**Step 1: Crear meta_controller.go**

```go
package events

import (
    "net/http"

    "events-stocks/repositories/eventsrepository"
    "events-stocks/utils"
    "github.com/labstack/echo/v4"
)

// GetEventMeta handles GET /api/events/:identifier/meta
// Public endpoint — returns minimal event data for OG meta tags.
// No auth required; safe to expose publicly.
func GetEventMeta(c echo.Context) error {
    identifier := c.Param("identifier")
    if identifier == "" {
        return utils.Error(c, http.StatusBadRequest, "Missing identifier", "")
    }

    event, err := eventsrepository.GetEventByIdentifier(identifier)
    if err != nil {
        return utils.Error(c, http.StatusNotFound, "Event not found", err.Error())
    }

    c.Response().Header().Set("Cache-Control", "public, max-age=3600")

    return utils.Success(c, http.StatusOK, "Event meta loaded", map[string]interface{}{
        "name":            event.Name,
        "cover_image_url": resolveCoverURL(event.CoverImageURL),
        "event_type":      event.EventType,
        "event_date_time": event.EventDateTime,
    })
}
```

> Nota: `resolveCoverURL` ya existe del Task 1. `event.Name`, `event.EventType`, `event.EventDateTime` son campos del modelo `models.Event` — verificar los nombres exactos leyendo `models/Event.go` si alguno falla la compilación.

**Step 2: Registrar la ruta en routes.go**

En `routes.go`, dentro del grupo `public` (sin auth middleware), agregar junto a las otras rutas de eventos:

```go
public.GET("/events/:identifier/meta", events.GetEventMeta)
```

Asegurarse de que quede ANTES de rutas con el mismo patrón `:identifier` si hay conflictos de orden.

**Step 3: Build**

```bash
cd /var/www/itbem-events-backend && go build ./...
```

Expected: sin errores.

**Step 4: Test**

```bash
# Usar un identifier real de un evento existente
curl http://localhost:8080/api/events/boda-andres-ivanna/meta | jq .
```

Expected:
```json
{
  "message": "Event meta loaded",
  "data": {
    "name": "Boda Andrés & Ivanna",
    "cover_image_url": "https://bucket.s3.amazonaws.com/events/uuid.webp?X-Amz-...",
    "event_type": "WEDDING",
    "event_date_time": "2026-04-15T18:00:00Z"
  }
}
```

**Step 5: Commit**

```bash
git add controllers/events/meta_controller.go routes/routes.go
git commit -m "feat(events): add public /meta endpoint for OG image data"
```

**Step 6: Deploy backend**

```bash
# Desde WSL
cd /var/www/itbem-events-backend
git push origin main
```

---

## Task 3: Dashboard — Actualizar `/api/og` para cover full + logo

El route actual usa cover al 30% de opacidad con overlay oscuro y texto. Hay que actualizarlo para: cuando hay cover → foto full + logo SVG esquina inferior derecha. Sin cover → mantener el diseño oscuro actual pero con logo en vez de texto "EventiApp".

**Files:**
- Modify: `C:\Users\AndBe\Desktop\Projects\dashboard-ts\src\app\api\og\route.tsx`

**Step 1: Leer el SVG del logo para conocer su estructura**

```bash
cat C:\Users\AndBe\Desktop\Projects\dashboard-ts\public\eventiapp-icon.svg
```

El logo es un SVG en `/public/eventiapp-icon.svg` (~1629 bytes). Se referenciará via URL construida desde `req.url` (funciona en edge runtime).

**Step 2: Reemplazar todo el contenido de route.tsx**

El nuevo comportamiento:
- **Con cover**: foto full (opacity 1, objectFit cover) + logo SVG en esquina inferior derecha (40×40px, con fondo blanco circular semitransparente)
- **Sin cover**: fondo degradado oscuro actual + logo centrado (fallback elegante)

```tsx
import { ImageResponse } from '@vercel/og'
import { NextRequest } from 'next/server'

export const runtime = 'edge'

export async function GET(req: NextRequest) {
    const { searchParams } = req.nextUrl
    const title = searchParams.get('title') || 'Evento'
    const date = searchParams.get('date') || ''
    const address = searchParams.get('address') || ''
    const cover = searchParams.get('cover') || ''
    const type = searchParams.get('type') || ''

    // Logo URL — served from same deployment (edge runtime compatible)
    const logoUrl = new URL('/eventiapp-icon.svg', req.url).toString()

    // ── Mode A: cover photo full + logo corner ─────────────────────────────
    if (cover) {
        return new ImageResponse(
            (
                <div
                    style={{
                        width: '100%',
                        height: '100%',
                        display: 'flex',
                        position: 'relative',
                    }}
                >
                    {/* Cover photo — full, no overlay */}
                    <img
                        src={cover}
                        alt=""
                        style={{
                            position: 'absolute',
                            top: 0,
                            left: 0,
                            width: '100%',
                            height: '100%',
                            objectFit: 'cover',
                        }}
                    />

                    {/* Subtle vignette so logo is readable on any photo */}
                    <div
                        style={{
                            position: 'absolute',
                            bottom: 0,
                            right: 0,
                            width: '160px',
                            height: '120px',
                            background: 'radial-gradient(ellipse at bottom right, rgba(0,0,0,0.45) 0%, transparent 70%)',
                            display: 'flex',
                        }}
                    />

                    {/* Logo — bottom-right corner */}
                    <div
                        style={{
                            position: 'absolute',
                            bottom: '24px',
                            right: '28px',
                            display: 'flex',
                            alignItems: 'center',
                            gap: '8px',
                        }}
                    >
                        <img
                            src={logoUrl}
                            width={36}
                            height={36}
                            style={{ borderRadius: '50%' }}
                            alt="eventiapp"
                        />
                        <span
                            style={{
                                fontSize: '18px',
                                fontWeight: 700,
                                color: '#ffffff',
                                textShadow: '0 1px 4px rgba(0,0,0,0.6)',
                                fontFamily: 'Inter, sans-serif',
                            }}
                        >
                            eventiapp
                        </span>
                    </div>
                </div>
            ),
            { width: 1200, height: 630 },
        )
    }

    // ── Mode B: no cover — dark branded fallback ───────────────────────────
    // Format date if provided
    let formattedDate = ''
    if (date) {
        try {
            formattedDate = new Intl.DateTimeFormat('es', {
                weekday: 'long', day: 'numeric', month: 'long', year: 'numeric',
            }).format(new Date(date))
        } catch {
            formattedDate = date
        }
    }

    return new ImageResponse(
        (
            <div
                style={{
                    width: '100%',
                    height: '100%',
                    display: 'flex',
                    position: 'relative',
                    backgroundColor: '#09090b',
                    fontFamily: 'Inter, sans-serif',
                }}
            >
                {/* Gradient overlay */}
                <div
                    style={{
                        position: 'absolute',
                        top: 0, left: 0, width: '100%', height: '100%',
                        background: 'linear-gradient(135deg, #1a0a2e 0%, #09090b 60%, #0d1a2e 100%)',
                        display: 'flex',
                    }}
                />

                {/* Content */}
                <div
                    style={{
                        position: 'relative',
                        width: '100%',
                        height: '100%',
                        display: 'flex',
                        flexDirection: 'column',
                        justifyContent: 'flex-end',
                        padding: '60px',
                    }}
                >
                    {type && (
                        <div style={{ display: 'flex', marginBottom: '16px' }}>
                            <span style={{ fontSize: '18px', fontWeight: 600, color: '#818cf8', textTransform: 'uppercase', letterSpacing: '2px' }}>
                                {type}
                            </span>
                        </div>
                    )}

                    <div style={{ fontSize: title.length > 30 ? '52px' : '64px', fontWeight: 700, color: '#ffffff', lineHeight: 1.1, marginBottom: '24px', display: 'flex' }}>
                        {title}
                    </div>

                    <div style={{ display: 'flex', gap: '32px', alignItems: 'center' }}>
                        {formattedDate && (
                            <div style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>
                                <span style={{ fontSize: '22px' }}>📅</span>
                                <span style={{ fontSize: '22px', color: '#a1a1aa', fontWeight: 500 }}>{formattedDate}</span>
                            </div>
                        )}
                        {address && (
                            <div style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>
                                <span style={{ fontSize: '22px' }}>📍</span>
                                <span style={{ fontSize: '22px', color: '#a1a1aa', fontWeight: 500 }}>
                                    {address.length > 40 ? address.slice(0, 40) + '...' : address}
                                </span>
                            </div>
                        )}
                    </div>

                    {/* Branding — bottom-right */}
                    <div
                        style={{
                            position: 'absolute',
                            bottom: '24px',
                            right: '28px',
                            display: 'flex',
                            alignItems: 'center',
                            gap: '8px',
                        }}
                    >
                        <img src={logoUrl} width={36} height={36} style={{ borderRadius: '50%' }} alt="eventiapp" />
                        <span style={{ fontSize: '18px', fontWeight: 700, color: '#ffffff', fontFamily: 'Inter, sans-serif' }}>
                            eventiapp
                        </span>
                    </div>
                </div>
            </div>
        ),
        { width: 1200, height: 630 },
    )
}
```

**Step 3: TypeScript check**

```bash
cd C:\Users\AndBe\Desktop\Projects\dashboard-ts
npx tsc --noEmit
```

Expected: 0 errores.

**Step 4: Test local del OG route**

```bash
npm run dev
```

Abrir en browser:
```
http://localhost:3000/api/og?cover=https://upload.wikimedia.org/wikipedia/commons/thumb/4/47/PNG_transparency_demonstration_1.png/280px-PNG_transparency_demonstration_1.png&title=Boda+Test
```

Expected: imagen 1200×630 con la foto a full y logo eventiapp esquina inferior derecha.

También probar sin cover:
```
http://localhost:3000/api/og?title=Boda+Andr%C3%A9s+%26+Ivanna&type=WEDDING&date=2026-04-15
```

Expected: fondo oscuro con título, tipo, y logo esquina inferior derecha.

**Step 5: Build de producción**

```bash
npm run build
```

Expected: build exitoso sin errores.

**Step 6: Commit y push**

```bash
git add src/app/api/og/route.tsx
git commit -m "feat(og): cover-full mode with logo corner + keep dark fallback"
git push origin main
```

---

## Task 4: Verificar Astro env var y test end-to-end

**Files:**
- Read: `C:\Users\AndBe\Desktop\Projects\cafetton-casero\.env` (o `.env.production`)
- Modify if missing: `.env`

**Step 1: Verificar que PUBLIC_DASHBOARD_URL está configurada**

```bash
grep PUBLIC_DASHBOARD_URL C:\Users\AndBe\Desktop\Projects\cafetton-casero\.env
```

Si no existe o está vacía, agregar:
```
PUBLIC_DASHBOARD_URL=https://dashboard.eventiapp.com
```

(Reemplazar con el dominio real del dashboard en Vercel.)

**Step 2: Verificar momentos.astro pasa cover a buildOgImageUrl**

Leer `src/pages/e/[identifier]/momentos.astro` y confirmar que:
1. Llama `fetchEventOgData(EVENTS_URL, identifier)`
2. Pasa `cover: ogData?.cover_image_url` a `buildOgImageUrl`
3. El resultado se pasa como `ogImage` al layout

Si falta alguno de estos pasos, corregir en `momentos.astro`.

**Step 3: Test end-to-end con WhatsApp**

Compartir en WhatsApp:
```
https://eventiapp.com/e/<identifier>/momentos
```

Expected: preview muestra foto de portada del evento a full con logo eventiapp esquina inferior derecha.

Si WhatsApp muestra preview viejo (cachea agresivamente), probar con un link nuevo o usar:
- [Meta Sharing Debugger](https://developers.facebook.com/tools/debug/) para forzar re-scrape

**Step 4: Commit Astro si se modificó**

```bash
cd C:\Users\AndBe\Desktop\Projects\cafetton-casero
git add src/pages/e/
git add .env  # solo si se agregó variable
git commit -m "feat(og): wire PUBLIC_DASHBOARD_URL for branded OG images"
git push origin main
```
